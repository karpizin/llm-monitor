# LLM Monitor (OpenRouter Watchdog)

Сервис для мониторинга доступности, качества и скорости работы LLM-моделей через OpenRouter API.
**Основной фокус MVP:** Мониторинг **бесплатных** (`:free`) моделей.

## 🎯 Проблема
Бесплатные модели на OpenRouter — отличный ресурс для разработки и тестов, но они нестабильны:
1.  **Исчезновение:** Модель может пропасть из списка доступных без предупреждения.
2.  **Downtime:** Провайдеры часто перегружены, возвращают 429 или 500.
3.  **Деградация:** Модель может начать отвечать пустыми строками или мусором ("I am a good AI assistant...").

## 💡 Решение
Автоматизированный "сторож" (Watchdog), который:
1.  Регулярно запрашивает список доступных бесплатных моделей.
2.  Отправляет тестовые запросы (Ping) к каждой модели.
3.  Фиксирует метрики: статус (OK/Error), латентность (TTFT), качество ответа.
4.  Предоставляет API/Дашборд с текущим статусом "Кто жив".

## 🛠 Технический стек (Планируемый)
*   **Backend/Poller:** Go (для эффективной конкурентной проверки множества моделей) или Python.
*   **Storage:** SQLite (для простоты MVP) или Prometheus/VictoriaMetrics.
*   **Frontend:** Simple Web Dashboard (Vue.js / HTML).
