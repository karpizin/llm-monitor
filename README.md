# LLM Monitor (OpenRouter Watchdog)

Сервис для автоматического мониторинга доступности, задержки и качества бесплатных LLM-моделей на OpenRouter.

## 🚀 Возможности
- **Discovery**: Автоматическое обнаружение новых бесплатных моделей в API OpenRouter.
- **Health Check**: Регулярная проверка доступности моделей (каждые 10 минут).
- **VLM Support**: Отдельный мониторинг мультимодальных моделей (Vision).
- **SQLite Storage**: Сохранение истории проверок для последующего анализа.
- **Docker Ready**: Запуск одной командой.

## 📦 Быстрый старт

### 1. Подготовка
Клонируйте репозиторий и создайте файл `.env`:
```bash
cp .env.example .env
```
Отредактируйте `.env`, вставив ваш `OPENROUTER_API_KEY`.

### 2. Запуск через Docker (Рекомендуется)
```bash
docker-compose up -d --build
```
Сервис начнет работу в фоновом режиме. База данных будет сохранена в папке `./data/monitor.db`.

### 3. Локальный запуск (без Docker)
```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python main_loop.py
```

## 📊 Структура данных (monitor.db)
- **`models`**: Список обнаруженных моделей, их контекст и тип (VLM/Free).
- **`checks`**: История "пингов": `latency_ms`, `status_code`, `success` и текст ошибки.

## 🛠 Технологии
- **Python 3.11**
- **SQLite**
- **Docker & Docker Compose**
- **Schedule** (планировщик задач)

## 🗺 Roadmap
- [ ] Визуализация метрик (простой веб-дашборд).
- [ ] Уведомления в Telegram при падении популярных моделей.
- [ ] Сравнение провайдеров для одной и той же модели.
- [ ] Оценка качества ответа (Sanity Check).
