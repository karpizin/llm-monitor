version: '3.8'

services:
  monitor:
    build: .
    container_name: llm-monitor
    restart: unless-stopped
    volumes:
      - ./data:/data
    env_file:
      - .env
    environment:
      - DB_PATH=/data/monitor.db
      - PYTHONUNBUFFERED=1
